.modal-body
    form([formGroup]="formName")
        io-input([data]="groupNameInput")
.modal-footer
    button.btn.btn-primary.btn-light(type="button", (click)="cancel()") {{ 'common.cancel' | translate }}
    button.btn.btn-primary.btn-primary(type="button", (click)="confirm()") {{ 'common.confirm' | translate }}
