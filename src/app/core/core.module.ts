import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';

/**
 * Application core module that will be imported in every domain module. It imports angular's common module.
 */
@NgModule({
    declarations: [],
    imports: [CommonModule],
})
export class CoreModule {}
